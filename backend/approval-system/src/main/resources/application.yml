server:
  port: 8080
  servlet:
    context-path: /api

spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/approval_db}
    username: ${SPRING_DATASOURCE_USERNAME:approval_user}
    password: ${SPRING_DATASOURCE_PASSWORD:approval_pass}
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

  mail:
    host: smtp.gmail.com
    port: 587
    username: truongminhtrang012@gmail.com
    password: xkvbfnukdtgwsnlo
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

app:
  jwt:
    secret: ApprovalSystemSecretKey2024VeryLongSecret!
    expiration: 86400000

s3:
  url: ${S3_URL:http://localhost:9000}
  accessKey: ${S3_ACCESS_KEY:approval_admin}
  secretKey: ${S3_SECRET_KEY:approval_secret_123}
  bucketName: ${S3_BUCKET:approval-system-attachments}